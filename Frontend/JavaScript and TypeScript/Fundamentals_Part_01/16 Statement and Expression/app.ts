let num2 :number = 8+8;
let bool : Boolean = false && true;

// anything which returns you a value is an expression
// anything which does not return you a value is a statement
// if statement is a statement, but the condition inside it is an expression


if(num2 > 10) {
    console.log("num2 is greater than 10");
}