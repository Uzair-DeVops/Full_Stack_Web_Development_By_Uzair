let a = "uzair"
let b = 3
let c = true

// the concept of type undefine when you just initizale the var but dont give it any value then automatically value undefined  will be stored in it and its type will also become undefine 
let d // undefined
console.log(typeof d) // undefined

// we will discuss null later on 